function validation() {
    var min_date = document.forms["questionform"]["min_date"]
    var max_date = document.forms["questionform"]["max_date"]
    var category = document.forms["questionform"]["category"]

    // compare min_date, max_date

    // find category
}
